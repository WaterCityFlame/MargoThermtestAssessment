<Application x:Class="MargoThermtestAssessment.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MargoThermtestAssessment"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <Style x:Key="headerStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="DarkOrange" />
        </Style>
        <Style x:Key="headerTitleStyle" TargetType="{x:Type Label}">
            <Setter Property="FontFamily" Value="Impact" />
            <Setter Property="Margin" Value="15,0,0,0"/>
            <Setter Property="FontSize" Value="32"/>

            <Setter Property="Foreground" Value="Navy"/>
        </Style>
        <Style x:Key="headerSubTitleStyle" TargetType="{x:Type Label}">
            <Setter Property="FontFamily" Value="Trebuchet MS" />
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="15,0,0,0"/>
            <Setter Property="Foreground" Value="Navy"/>
        </Style>
    </Application.Resources>
</Application>
